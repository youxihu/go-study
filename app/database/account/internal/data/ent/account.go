// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"go-study/app/database/account/internal/data/ent/account"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Account is the model entity for the Account schema.
type Account struct {
	config `json:"-"`
	// ID of the ent.
	// 用户id
	ID int32 `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID int32 `json:"tenant_id,omitempty"`
	// 更新信息时间
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 注册时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 当前积分
	AccountIntegral int32 `json:"account_integral,omitempty"`
	// 当前余额
	AccountBalance float64 `json:"account_balance,omitempty"`
	// 用户身份 1普通用户 2镖师 3业务员  4客服人员
	UserIdentity int8 `json:"user_identity,omitempty"`
	// 冻结资金
	FreezeBalance float64 `json:"freeze_balance,omitempty"`
	// 冻结积分
	FreezeIntegral int32 `json:"freeze_integral,omitempty"`
	selectValues   sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Account) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case account.FieldAccountBalance, account.FieldFreezeBalance:
			values[i] = new(sql.NullFloat64)
		case account.FieldID, account.FieldTenantID, account.FieldAccountIntegral, account.FieldUserIdentity, account.FieldFreezeIntegral:
			values[i] = new(sql.NullInt64)
		case account.FieldUpdatedAt, account.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Account fields.
func (a *Account) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case account.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int32(value.Int64)
		case account.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				a.TenantID = int32(value.Int64)
			}
		case account.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				a.UpdatedAt = value.Time
			}
		case account.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				a.CreatedAt = value.Time
			}
		case account.FieldAccountIntegral:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field account_integral", values[i])
			} else if value.Valid {
				a.AccountIntegral = int32(value.Int64)
			}
		case account.FieldAccountBalance:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field account_balance", values[i])
			} else if value.Valid {
				a.AccountBalance = value.Float64
			}
		case account.FieldUserIdentity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_identity", values[i])
			} else if value.Valid {
				a.UserIdentity = int8(value.Int64)
			}
		case account.FieldFreezeBalance:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field freeze_balance", values[i])
			} else if value.Valid {
				a.FreezeBalance = value.Float64
			}
		case account.FieldFreezeIntegral:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field freeze_integral", values[i])
			} else if value.Valid {
				a.FreezeIntegral = int32(value.Int64)
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Account.
// This includes values selected through modifiers, order, etc.
func (a *Account) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// Update returns a builder for updating this Account.
// Note that you need to call Account.Unwrap() before calling this method if this Account
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Account) Update() *AccountUpdateOne {
	return NewAccountClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Account entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Account) Unwrap() *Account {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Account is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Account) String() string {
	var builder strings.Builder
	builder.WriteString("Account(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", a.TenantID))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(a.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(a.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("account_integral=")
	builder.WriteString(fmt.Sprintf("%v", a.AccountIntegral))
	builder.WriteString(", ")
	builder.WriteString("account_balance=")
	builder.WriteString(fmt.Sprintf("%v", a.AccountBalance))
	builder.WriteString(", ")
	builder.WriteString("user_identity=")
	builder.WriteString(fmt.Sprintf("%v", a.UserIdentity))
	builder.WriteString(", ")
	builder.WriteString("freeze_balance=")
	builder.WriteString(fmt.Sprintf("%v", a.FreezeBalance))
	builder.WriteString(", ")
	builder.WriteString("freeze_integral=")
	builder.WriteString(fmt.Sprintf("%v", a.FreezeIntegral))
	builder.WriteByte(')')
	return builder.String()
}

// Accounts is a parsable slice of Account.
type Accounts []*Account
